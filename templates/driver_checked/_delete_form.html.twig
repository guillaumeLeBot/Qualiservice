<form method="post" action="{{ path('app_driver_checked_delete', {'id': driver_checked.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ driver_checked.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}New Calendar{% endblock %}

{% block body %}
    <div class="container">
    <h1>Création d'un évènement</h1>
        {{ include('calendar/_form.html.twig') }}

        <a class="btn btn-info" href="{{ path('calendar_index') }}">Retour à la liste</a>
    </div>

{% endblock %}

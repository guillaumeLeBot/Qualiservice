{% extends 'base.html.twig' %}

{% block title %}Edit Calendar{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h1>Modification</h1>
                <a class="btn btn-info" href="{{ path('app_calendar') }}">Retour au calendrier</a>
                {{ include('calendar/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            </div>
            <div class="col-md-6">
                {{ include('calendar/_delete_form.html.twig') }}
            </div>
        </div>


    </div>
{% endblock %}
